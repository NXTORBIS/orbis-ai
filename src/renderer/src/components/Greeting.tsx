/** Simple greeting like Claude Code - clean and minimal. */
export default function Greeting({ title }: { title: string }): React.JSX.Element {
  return (
    <h1 className="greeting">
      Where should we begin?
    </h1>
  )
}
